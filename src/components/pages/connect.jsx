import { Typography } from "@material-ui/core";

const Connect = () => {
    return (
        <div>
            <Typography>
                Connect
            </Typography>
        </div>
    );
};
export default Connect;