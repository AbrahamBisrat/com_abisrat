import { Typography } from "@material-ui/core";

const Connect = () => {
    return (
        <div id='connect'>
            <Typography>
                Connect
            </Typography>
        </div>
    );
};
export default Connect;