import { Typography } from "@material-ui/core";

const Profile = () => {
    return (
        <div>
            <Typography>
                Profile
            </Typography>
        </div>
    );
};
export default Profile;