import { Typography } from "@material-ui/core";

const profileStyle = {
    height: '100vh',
}

const Profile = () => {
    return (
        <div style={profileStyle} id='profile'>
            <Typography>
                Profile
            </Typography>
        </div>
    );
};
export default Profile;